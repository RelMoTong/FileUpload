# 图片异步上传工具 - 全库代码审查报告

- 审查日期：2026-08-10
- 审查方式：全库快速审查（只读，未修改任何代码）
- 项目版本：v3.5.1（`src/__init__.py`）
- 验证情况：`pytest` 94 passed / 5 subtests passed；`compileall` 与 AST 解析通过

> 结论：整体架构与工程质量较好（MVC 分层清晰、有边界测试、类型注解完整），
> 但存在 1 个高风险数据丢失点、多处安全弱点和若干中等逻辑问题，详见下文。

---

## 问题清单（按严重级别）

每个问题包含：文件 / 位置、问题、影响、建议修复、是否立即修。

---

### 高风险

#### 1. 自动清理完全忽略“保留天数”，会删掉新文件

- **文件/位置**：`src/services/cleanup_service.py` `run_auto_cleanup`（约 590 行）；`src/ui/dialogs/disk_cleanup_dialog.py:1368`（保留天数控件被禁用并标注“不生效”）
- **问题**：自动清理按“最旧优先”删除所有匹配格式的文件，直到磁盘占用降到目标阈值，没有任何“只删 N 天前文件”的过滤。配置字段 `auto_delete_keep_days` 仅作兼容保留（`src/ui/main_window.py:302` 注释明确“不参与自动清理筛选”）。
- **影响**：生产环境工具一旦启用自动清理且阈值配置不当，当天的新图片也可能被删除。这是**有意设计**，但风险极高。
- **建议修复**：真正实现 `keep_days` 过滤（推荐），或在启用自动清理时给出醒目的二次确认/警告，说明“不受保留天数限制”，并在删除前再次校验文件 mtime。
- **是否立即修**：是；否则应保持该功能默认关闭。

#### 2. 认证密码用无盐 SHA-256 + 硬编码默认密码

- **文件/位置**：`src/services/auth_service.py`（`hash_password`、`DEFAULT_USER_PASSWORD_HASH`、`SIMPLE_PASSWORDS`）
- **问题**：`hashlib.sha256(password)` 无盐存储；默认密码硬编码为 `"123"`（用户）和 `"Tops123"`（管理员），明文写在源码中。
- **影响**：无盐哈希可被彩虹表/暴力破解，默认口令可查源码得知。本地工具风险有限，但属明显安全弱点。
- **建议修复**：改用 `hashlib.pbkdf2_hmac` / `scrypt` 加盐存储（格式如 `pbkdf2$salt$hash`）；保留旧无盐哈希迁移路径（登录成功后自动升级）；默认密码首次登录强制修改。
- **是否立即修**：建议尽快，改动不大且有测试保护。

#### 3. 断点续传完成时“先删目标再改名”，存在丢文件窗口

- **文件/位置**：`src/core/resume_manager.py` `upload_with_resume`（约 505 行）
- **问题**：`if os.path.exists(target_path): os.remove(target_path)` 后再 `os.rename(temp_file, target_path)`；rename 因网络/跨卷等原因失败时，目标文件已被删除，只剩 `.part` 临时文件。
- **影响**：网络共享场景下可能造成目标文件丢失。
- **建议修复**：改用 `os.replace()` 语义并在失败时保留原文件（先备份），或改名失败时回退为 `copy2` + 校验。
- **是否立即修**：是（涉及网络共享场景）。

---

### 中风险

#### 4. FTP 测试连接在 GUI 线程阻塞最多约 30 秒

- **文件/位置**：`src/protocols/ftp.py` `FTPClientUploader.connect`（约 300 行）
- **问题**：重试循环在持有 `self._lock` 的情况下 sleep 5/10/15 秒；“测试连接”按钮直接在 GUI 线程调用，网络不通时界面冻结近半分钟。
- **建议修复**：测试放到后台线程/QThread，或把重试等待改为可中断的短间隔。
- **是否立即修**：建议。

#### 5. 保存配置非原子写，崩溃可能损坏 config.json

- **文件/位置**：`src/config.py` `save`（约 140 行）；`load`（约 110 行）
- **问题**：直接 `open(..., 'w')` 写入；写入中途断电/崩溃会损坏配置文件；下次 `load()` 静默用默认配置覆盖（JSON 解析失败时用户账号无法保留）。
- **建议修复**：先写临时文件再 `os.replace()`；发现损坏时把坏文件备份为 `config.json.bak` 再重建。
- **是否立即修**：建议。

#### 6. `resume_min_size_mb` 配置从未生效

- **文件/位置**：`src/models/upload_settings.py:44`；`src/workers/upload_worker.py:938`
- **问题**：配置项有读写，但 Worker 始终用固定的 `ResumeManager.MIN_RESUME_SIZE`（10MB），用户修改配置无效。
- **建议修复**：把 `resume_min_size_mb` 传入 `UploadTaskRequest`/Worker，或从 UI 移除该选项。
- **是否立即修**：建议。

#### 7. 断点续传声称支持 FTP，实际只做本地复制

- **文件/位置**：`src/core/resume_manager.py` `ResumableFileUploader` 文档字符串
- **问题**：该类只是本地文件拷贝 + 改名，没有 FTP 传输逻辑；FTP 模式下大文件仍是整传，无续传。
- **建议修复**：修正文档，或在 FTP 路径实现真正的断点续传（记录已传字节、使用 `REST`）。
- **是否立即修**：可选。

#### 8. FTP TLS 缺少证书配置，功能大概率不可用

- **文件/位置**：`src/protocols/ftp.py` `start()`（约 210 行）；`src/models/ftp_settings.py`
- **问题**：UI 有“启用 TLS”开关，但配置里没有证书/密钥路径，代码用默认 `cert.pem`/`key.pem`（当前工作目录）；文件不存在时 TLS 握手会失败（需验证：取决于 pyftpdlib 版本，可能启动即报错或连接时报错）。
- **建议修复**：提供证书选择 UI；或没有证书时禁用 TLS 选项并提示。
- **是否立即修**：建议（`needs verification`）。

#### 9. 文件操作“超时”不会终止底层线程

- **文件/位置**：`src/workers/upload_worker.py` `_safe_path_operation`、`_get_image_files`
- **问题**：`future.result(timeout=...)` 超时后 `future.cancel()` 只取消排队，不中断正在运行的 `os.walk`/网络操作；反复超时会堆积后台线程（`_rebuild_executor` 只能缓解）。
- **建议修复**：对扫描改用可协作取消的遍历；对网络文件操作设置更合理的超时并记录告警。
- **是否立即修**：可选。

#### 10. 去重开启时对目标目录全量哈希，性能可能很差

- **文件/位置**：`src/workers/upload_worker.py` `_find_duplicate_by_hash`
- **问题**：每个周期、每个新文件都 `os.walk` 目标目录并逐个计算 MD5/SHA256，目录大时 CPU/IO 开销明显。
- **建议修复**：维护文件哈希索引（按大小 + 修改时间预筛），或只对同大小文件做哈希。
- **是否立即修**：可选。

#### 11. FTP 默认明文传输 + 服务器用户拥有全部权限

- **文件/位置**：`src/protocols/ftp.py`（`perm='elradfmwMT'`、`enable_tls` 默认 False）
- **问题**：FTP 客户端和服务器的 `enable_tls` 默认关闭，口令明文过网；服务器用户具备删除/重命名/改权限等全部操作权限。
- **建议修复**：默认开启 TLS（提供证书）；权限按需收紧；UI 明确提示明文风险。属设计取舍，至少应有提示。
- **是否立即修**：建议。

---

### 低风险 / 待确认

#### 12. 自动清理结束状态不准

- **文件/位置**：`src/services/cleanup_service.py` `run_auto_cleanup`
- **问题**：候选文件删完仍未达到目标时，状态记为“无候选文件”，审计日志会误导。
- **建议修复**：区分“已耗尽候选”与“无候选”。

#### 13. 死代码

- **文件/位置**：`src/services/cleanup_service.py:497` `select_cleanup_candidates`
- **问题**：定义后无任何调用；返回的第二个值 `len(ordered)`（全部候选数）语义存疑。
- **建议修复**：删除或补测试。

#### 14. 非 Windows 平台密码“加密”字段存明文

- **文件/位置**：`src/controllers/settings_controller.py` `encode_ftp_password`
- **问题**：非 win32 时 `protect_secret` 原样返回，明文存进 `password_encrypted`；Windows 上 DPAPI 又绑定当前用户，配置拷到别的机器解不开。属已知取舍。
- **建议修复**：补充注释说明，或非 Windows 平台改为显式警告。

#### 15. 去重关闭时“目标已存在”的跳过文件永不归档

- **文件/位置**：`src/workers/upload_worker.py`（约 1530 行）
- **问题**：不归档导致源文件残留、每轮重复扫描并累加 skipped 计数；与去重开启时“跳过即归档”行为不一致。
- **建议修复**：统一处理或提示用户。

#### 16. FTP 事件日志无锁

- **文件/位置**：`src/repositories/ftp_event_log_repository.py`
- **问题**：多个 FTP 连接并发写同一日志可能交错（`CleanupAuditRepository` 有锁，这里没有）。
- **建议修复**：加线程锁。

#### 17. 跨线程直接调用 Worker 方法

- **文件/位置**：`src/services/upload_service.py`（`pause` / `resume` / `stop`）
- **问题**：GUI 线程直接调用位于 QThread 中的 worker 对象方法；目前只是布尔赋值，靠 GIL 勉强安全，但模式脆弱。
- **建议修复**：改为 QueuedConnection 调用。

#### 18. 续传进度每 1MB 全量重写 JSON 记录

- **文件/位置**：`src/core/resume_manager.py` `update_progress`
- **问题**：大文件会产生大量磁盘写（1GB 文件约 1000 次全量重写）。
- **建议修复**：按时间/百分比节流。

---

## 做得好的地方

- 分层清晰：Controller 不碰 Qt，Service 可独立测试；协议类都有 Protocol 抽象，测试替身容易。
- 退出编排（`LifecycleController`）考虑超时、恢复界面、Qt 退出确认，细节到位。
- 清理删除默认走回收站、跨盘校验、审计日志、失败上限（20 次）等防护齐全。
- 上传/归档分离、断点续传记录校验（源路径/大小/临时文件一致性）写得很扎实。

---

## 修复计划（建议顺序）

1. **立即**：自动清理的 `keep_days` 要么实现、要么在 UI 显著警告（风险最高）。
2. **本周**：密码改加盐哈希并迁移旧哈希；断点续传完成阶段改用安全替换。
3. **近期**：FTP 测试连接移出 GUI 线程；配置保存改原子写；`resume_min_size_mb` 接线或移除；TLS 证书配置补全或禁用。
4. **顺手**：清理死代码（`select_cleanup_candidates`）、FTP 日志加锁、统一去重跳过的归档行为、续传进度节流。

---

*本报告为 2026-08-10 全库快速审查的快照，代码如有变更请以最新状态为准。*
