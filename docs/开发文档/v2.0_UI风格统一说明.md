# v2.0 UI 风格统一说明

**文档版本**: v1.0  
**更新日期**: 2025-02-25  
**适用范围**: v2.0 所有 UI 组件

---

## 🎨 设计原则

### 核心理念
**与 pyqt_app.py 保持 100% 风格一致**

v2.0 的所有新增 UI 组件必须遵循现有的设计系统，确保用户体验的连贯性和专业性。

---

## 📐 设计规范

### 1. 颜色方案

#### 主色调（蓝色系）
```css
主色：    #1976D2  /* 主要按钮、标题 */
浅蓝：    #64B5F6  /* 边框、分割线 */
极浅蓝：  #E3F2FD  /* 背景色 */
悬停：    #1E88E5  /* 按钮悬停 */
渐变起点： #4FACFE  /* 进度条渐变 */
渐变终点： #00F2FE  /* 进度条渐变 */
```

#### 辅助色
```css
文字主色：  #1F2937  /* 正文 */
文字辅色：  #6B7280  /* 提示文字 */
文字禁用：  #9CA3AF  /* 禁用状态 */

背景白：    #FFFFFF  /* 卡片背景 */
背景灰：    #F3F4F6  /* 禁用/只读背景 */
边框灰：    #D1D5DB  /* 禁用边框 */

成功绿：    #166534  /* 成功状态 */
警告黄：    #A16207  /* 警告状态 */
危险红：    #B91C1C  /* 错误/删除 */
```

### 2. 字体规范

#### 字体家族
```css
font-family: 'Segoe UI', 'Microsoft YaHei UI'
```

#### 字号层级
```css
标题：    14pt  (font-weight: 700)
正文：    11pt  (默认)
提示：    10pt  (可选)
```

### 3. 圆角规范

```css
卡片：        10px  (border-radius)
按钮：        8px   (border-radius)
输入框：      4px   (border-radius)
进度条：      6px   (border-radius)
分组框：      6px   (border-radius)
```

### 4. 间距规范

```css
按钮内边距：
  Primary:    8px 12px
  Secondary:  6px 10px
  
输入框内边距：  4px

卡片内边距：    12px
```

---

## 🎯 组件样式类

### 按钮样式类

#### Primary（主要按钮）
```python
btn.setProperty("class", "Primary")
```
- 背景：#1976D2（蓝色）
- 文字：#FFFFFF（白色）
- 用途：主要操作（启动、保存、确认）

#### Secondary（次要按钮）
```python
btn.setProperty("class", "Secondary")
```
- 背景：#F1F5F9（浅灰）
- 边框：#64B5F6（蓝色）
- 用途：辅助操作（测试、新增、浏览）

#### Warning（警告按钮）
```python
btn.setProperty("class", "Warning")
```
- 背景：#FEF3C7（浅黄）
- 文字：#A16207（深黄）
- 用途：警告操作（暂停）

#### Danger（危险按钮）
```python
btn.setProperty("class", "Danger")
```
- 背景：#FEE2E2（浅红）
- 文字：#B91C1C（深红）
- 用途：危险操作（删除、停止）

### 标签样式类

#### Title（标题）
```python
label.setProperty("class", "Title")
```
- 颜色：#1976D2（蓝色）
- 字号：14pt
- 字重：700（粗体）

### 容器样式

#### Card（卡片）
```python
frame.setObjectName("Card")
```
- 背景：#FFFFFF（白色）
- 边框：2px solid #64B5F6
- 圆角：10px
- 内边距：12px

---

## 💻 代码示例

### 正确示例（v2.0 风格）

```python
from PySide6.QtWidgets import QFrame, QLabel, QPushButton, QVBoxLayout

# 创建卡片容器
card = QFrame()
card.setObjectName("Card")  # 使用 Card 样式
layout = QVBoxLayout(card)

# 标题
title = QLabel("FTP 服务器配置")
title.setProperty("class", "Title")  # 使用 Title 样式类
layout.addWidget(title)

# 主要按钮
start_btn = QPushButton("启动服务器")
start_btn.setProperty("class", "Primary")  # 使用 Primary 样式类

# 危险按钮
stop_btn = QPushButton("停止服务器")
stop_btn.setProperty("class", "Danger")  # 使用 Danger 样式类

# 状态文字（成功）
status_label = QLabel("✓ 运行中")
status_label.setStyleSheet("color: #166534; font-weight: bold;")

# 状态文字（错误）
error_label = QLabel("未连接")
error_label.setStyleSheet("color: #B91C1C; font-weight: bold;")

# 提示文字
hint_label = QLabel("请选择配置")
hint_label.setStyleSheet("color: #6B7280;")
```

### ❌ 错误示例（不要使用）

```python
# ❌ 不要自定义绿色/红色按钮
btn.setStyleSheet("background-color: #4CAF50; color: white;")  # 错误！

# ❌ 不要使用不一致的颜色
label.setStyleSheet("color: green;")  # 错误！应该用 #166534

# ❌ 不要使用不一致的字号
label.setStyleSheet("font-size: 14px;")  # 错误！应该用 14pt

# ❌ 不要忽略卡片容器
layout = QVBoxLayout(self)  # 错误！应该先创建 Card Frame
```

---

## 📋 完整主题 StyleSheet

### 应用方法

在测试代码或主程序中应用：

```python
window.setStyleSheet("""
    QWidget{font-family:'Segoe UI', 'Microsoft YaHei UI'; font-size:11pt; color:#1F2937; background:#E3F2FD;}
    QMainWindow{background:#E3F2FD;}
    QFrame#Card{background:#FFFFFF; border:2px solid #64B5F6; border-radius:10px; padding: 12px;}
    QLabel{color:#1F2937;}
    QLabel.Title{color:#1976D2; font-weight:700; font-size:14pt;}
    
    /* 按钮样式 */
    QPushButton{font-size:11pt;}
    QPushButton:disabled{background:#E5E7EB; color:#9CA3AF; border:1px solid #D1D5DB;}
    QPushButton.Primary{background:#1976D2; color:#FFFFFF; border:none; border-radius:8px; padding:8px 12px;}
    QPushButton.Primary:hover{background:#1E88E5;}
    QPushButton.Primary:disabled{background:#BDBDBD; color:#FFFFFF;}
    QPushButton.Secondary{background:#F1F5F9; color:#0F172A; border:1px solid #64B5F6; border-radius:8px; padding:6px 10px;}
    QPushButton.Secondary:hover{background:#E3F2FD;}
    QPushButton.Danger{background:#FEE2E2; color:#B91C1C; border:1px solid #FCA5A5; border-radius:8px; padding:6px 10px;}
    QPushButton.Danger:hover{background:#FECACA;}
    
    /* 输入控件样式 */
    QLineEdit{background:#FFFFFF; color:#1F2937; border:1px solid #64B5F6; border-radius:4px; padding:4px;}
    QSpinBox{background:#FFFFFF; color:#1F2937; border:1px solid #64B5F6; border-radius:4px; padding:4px;}
    QComboBox{background:#FFFFFF; color:#1F2937; border:1px solid #64B5F6; border-radius:4px; padding:4px;}
    
    /* 分组框样式 */
    QGroupBox{color:#1976D2; font-weight:600; border:1px solid #64B5F6; border-radius:6px; margin-top:8px; padding-top:8px;}
    QGroupBox::title{subcontrol-origin:margin; left:10px; padding:0 5px;}
    
    /* 列表样式 */
    QListWidget{background:#FFFFFF; border:1px solid #64B5F6; border-radius:4px;}
    QListWidget::item{padding:6px; border-radius:3px;}
    QListWidget::item:selected{background:#E3F2FD; color:#1976D2;}
    QListWidget::item:hover{background:#F1F5F9;}
""")
```

---

## ✅ 检查清单

在创建新的 UI 组件时，请确保：

- [ ] 使用 `QFrame` 并设置 `setObjectName("Card")` 作为容器
- [ ] 标题使用 `setProperty("class", "Title")`
- [ ] 按钮使用正确的样式类（Primary/Secondary/Warning/Danger）
- [ ] 颜色使用设计系统中定义的颜色值
- [ ] 字体大小使用 pt 而不是 px
- [ ] 圆角半径符合规范（10px/8px/6px/4px）
- [ ] 成功状态使用 #166534（绿色）
- [ ] 错误状态使用 #B91C1C（红色）
- [ ] 提示文字使用 #6B7280（灰色）
- [ ] 测试时应用完整的主题 StyleSheet

---

## 🔍 常见问题

### Q1: 为什么不能用 `color: green`？
**A**: 必须使用精确的十六进制颜色值以确保一致性。`green` 在不同系统上可能显示不同。使用 `#166534`（成功）或 `#B91C1C`（错误）。

### Q2: 按钮样式类不生效怎么办？
**A**: 确保：
1. 使用 `setProperty("class", "Primary")` 而不是 `setStyleSheet()`
2. 在主窗口应用了完整的主题 StyleSheet
3. 没有其他 `setStyleSheet()` 覆盖了样式类

### Q3: Card 边框不显示？
**A**: 确保：
1. 使用 `setObjectName("Card")` 而不是 `setProperty()`
2. 导入了 `QFrame` 而不是用 `QWidget`
3. 主题 StyleSheet 包含 `QFrame#Card` 规则

### Q4: 字体大小单位用 px 还是 pt？
**A**: **必须用 pt**。设计系统中所有字号都是 pt 单位（11pt、14pt），这样在不同 DPI 屏幕上都能正确缩放。

---

## 📊 风格对比

### 修复前（不一致）

```python
# 标题
title_label.setStyleSheet("font-size: 14px; font-weight: bold;")  # ❌

# 按钮
start_btn.setStyleSheet("background-color: #4CAF50; color: white;")  # ❌

# 状态
status_label.setStyleSheet("font-size: 12px; color: green;")  # ❌
```

### 修复后（一致）

```python
# 标题
title_label.setProperty("class", "Title")  # ✅

# 按钮
start_btn.setProperty("class", "Primary")  # ✅

# 状态
status_label.setStyleSheet("color: #166534; font-weight: bold;")  # ✅
```

---

## 📁 相关文件

- **参考实现**: `pyqt_app.py`（行 1168-1230）
- **FTP UI 组件**: `ftp_ui_component.py`（已更新）
- **主题定义**: 见上方完整 StyleSheet

---

## 🎯 下一步

在创建新的 UI 组件时：

1. 复制本文档中的 StyleSheet 模板
2. 使用标准的样式类和颜色
3. 在独立窗口测试时应用主题
4. 集成到主程序前验证风格一致性

---

**维护责任**: 所有 v2.0 UI 开发人员  
**审查标准**: 必须通过风格一致性检查  
**更新频率**: 随设计系统演进

---

*本文档确保 v2.0 的所有 UI 组件保持专业、一致的视觉体验。*
