# 图片异步上传工具 v3.5.1：待解决问题与发布验收清单

- 建立日期：2026-08-18
- 审查对象：本地工作区当前代码（包含未提交、未跟踪文件）
- 目标版本：下一现场发布版本
- 当前结论：**GO（带已记录的风险豁免）。本地代码与冻结包门禁通过；用户于 2026-08-24 明确对 24 小时长稳、真实超大文件、真实现场网络三项验收接受风险并跳过。这三项未执行，不能表述为“测试通过”。独立干净 Windows 验收仍按 2026-08-18 的既有风险接受记录单独保留。**
- 执行原则：先修复 P1 上线阻断问题，再处理 P2 稳定性与性能问题，最后完成打包和现场模拟验收。

## 1. 当前验证基线

- 最终全量回归：`159 passed, 5 subtests passed in 23.26s`。
- `compileall` 通过；pyright 1.1.411：`0 errors, 0 warnings, 0 informations`。
- PyInstaller 6.12.0 已从当前本地源码构建 v3.5.1；冻结包发布探针 11/11 通过。
- 180 秒最终成品长稳基线：181 个样本，RSS 首尾中位数下降 84 KiB，线程峰值 8，句柄峰值 332，日志增长 485 B，无残留进程。
- C 盘低空间依赖已通过磁盘状态注入消除，全部测试固定在 E 盘隔离目录执行。
- 本次风险接受（2026-08-24）：24 小时长稳、真实超大文件、真实现场网络三项验收均由用户明确接受风险并跳过；三项状态均为“未执行”，不得表述为“测试通过”。
- 既有风险接受（2026-08-18）：独立无开发环境 Windows 验收未执行，继续单独记录为接受风险并跳过。

## 2. P1：上线阻断问题

> P1 必须在正式打包发布前全部完成。每项修复都必须补充自动化测试。

### P1-01 FTP 连接与重试阻塞 GUI

- [x] 已修复（2026-08-18）
- 实施结果：FTP 客户端测试改由 `FTPService` 的单一守护后台任务执行，View 只通过 Qt 信号接收进度、成功、失败和取消事件；测试按钮在运行时切换为取消操作，退出准备阶段会主动取消。
- 连接取消：`FTPClientUploader` 不再在整个网络连接期间阻塞 `disconnect()`；取消会立即设置事件并关闭当前 socket，上传 Worker 也把停止事件传入连接过程。重试等待可由事件在 100ms 内唤醒，最后一次失败后不再等待。
- 生命周期：后台测试最多一个且可追踪，关闭时不做无界等待；测试线程为守护线程，底层连接超时也不会阻止进程退出。
- 验证证据：`pytest -q tests/test_ftp_mvc.py tests/test_ftp_basic.py tests/test_ftp_server_logging.py tests/test_upload_mvc.py tests/test_lifecycle_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p101`，41 passed；覆盖末次失败无等待、重试等待取消、连接锁不阻塞断开、异步测试取消和关闭返回。
- 位置：`src/ui/main_window.py:1731`、`src/protocols/ftp.py:424-505`、`src/services/upload_service.py:234-241`
- 问题：FTP 测试连接在 GUI 线程同步执行。默认 3 次连接、每次最多 30 秒，并额外等待 5、10、15 秒，最坏约 120 秒无响应。连接期间持有客户端锁，停止上传调用 `disconnect()` 时也可能被阻塞。
- 修复要求：
  - FTP 测试、连接和重试移入后台任务。
  - 提供可取消机制，停止程序时能立即发出取消请求。
  - 最后一次失败后不得继续等待。
  - GUI 只能接收进度、成功、失败和取消事件，不能直接执行网络连接。
- 验收标准：
  - 连接不存在的地址时，窗口仍可拖动、点击和关闭。
  - 点击停止后在限定时间内返回，不等待完整重试周期。
  - 新增失败重试、取消、关闭期间取消测试。

### P1-02 配置保存非原子且会覆盖损坏配置

- [x] 已修复（2026-08-18）
- 实施结果：配置在目标目录创建临时文件，完成 JSON 写入、`flush()`、`os.fsync()` 后使用 `os.replace()` 原子提交；任何失败都会清理临时文件并保留旧配置。
- 损坏恢复：解析失败时原 `config.json` 保持原样，并复制为带微秒时间戳的 `config.json.corrupt-*.bak`；Repository/Controller 的 `last_error` 包含原件保留与备份路径，界面明确记录错误并仅使用内存默认值。
- 数据保留：保存前深度合并已有有效配置，继续保留未知字段；已有用户哈希默认受保护，FTP 密文及 FTP 未知字段经类型模型往返不丢失。
- 验证证据：`pytest -q tests/test_settings_controller.py tests/test_mvc_models.py tests/test_auth_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p102`，29 passed；覆盖替换失败、损坏备份、用户哈希/FTP 密文/未知字段往返。
- 位置：`src/config.py:140-145`、`src/config.py:147-176`
- 问题：配置文件被直接截断写入；写入中断会留下损坏 JSON。下次加载失败时程序立即写入默认配置，可能覆盖原配置、FTP 参数和账号哈希。
- 修复要求：
  - 在同目录写入临时文件，完成 flush/fsync 后使用 `os.replace()` 原子替换。
  - 加载失败时不得覆盖原文件。
  - 将损坏文件保留为带时间戳的备份，并向界面返回明确错误。
  - 保持未知配置字段和已有用户信息。
- 验收标准：
  - 模拟写入中断后，旧配置仍可读取。
  - 损坏 JSON 启动时生成备份，不覆盖原文件。
  - 用户、FTP 密码密文和未知字段往返不丢失。

### P1-03 断点续传完成阶段存在目标文件丢失窗口

- [x] 已修复（2026-08-18）
- 实施结果：提交前校验 `.part` 文件长度；先将源文件属性复制到临时文件，再用同目录 `os.replace()` 原子替换目标。替换或属性复制失败时保留旧目标、完整临时文件与续传记录。
- 职责收敛：续传上传器负责原子提交并通知 `ResumeManager` 清理记录；`UploadWorker` 不再在成功返回后重复调用完成清理。
- 验证证据：`pytest -q tests/test_resume_without_size_limit.py tests/test_upload_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p103`，30 passed；覆盖已有目标原子替换、替换失败保留现场、长度不符拒绝提交、中断后继续上传及 Worker 不重复清理。
- 位置：`src/core/resume_manager.py:467-478`
- 问题：代码先删除原目标文件，再把 `.part` 文件改名为目标文件；网络、权限或改名失败时，旧目标已经丢失。
- 修复要求：
  - 提交前校验临时文件长度，必要时校验哈希。
  - 使用同目录 `os.replace()` 原子提交。
  - 提交失败时保留旧目标和续传临时文件。
  - 消除 Worker 与 ResumeManager 重复执行完成清理的逻辑。
- 验收标准：
  - 注入替换失败时，旧目标内容保持不变。
  - 成功替换后目标内容完整，续传记录正确删除。
  - 中断后可继续上传，不产生丢失或重复完成状态。

### P1-04 清理删除前没有严格验证文件身份

- [x] 已修复（2026-08-18）
- 实施结果：手动扫描与自动索引均保存文件大小、`mtime_ns` 和可用的文件系统标识；删除前重新 `stat`，任一关键属性变化即保留文件、刷新自动索引并写入 `DELETE_SKIP_CHANGED` 审计事件。
- 兼容处理：清理索引升级为 schema v2；检测到 v1 表时安全失效旧就绪标记并清空旧记录，要求重新建立基线索引。自动清理排序及文案统一为“全局修改时间最旧优先”。
- 验证证据：`pytest -q tests/test_cleanup_policy_and_startup.py tests/test_cleanup_mvc.py tests/test_cleanup_index.py tests/test_architecture_boundaries.py -p no:cacheprovider`，42 passed；包含手动覆盖后跳过、自动修改后跳过及审计、v1 迁移失效、嵌套目录全局排序测试。
- 位置：`src/services/cleanup_service.py:188-219`、`src/services/cleanup_service.py:963-1023`
- 问题：手工删除只凭扫描时记录的路径执行；自动清理主要比较创建时间。文件在扫描后被覆盖或更新但创建时间不变时，可能删除刚写入的新内容。
- 修复要求：
  - 扫描/索引记录至少保存 `mtime_ns`、大小和可用的文件标识。
  - 删除前重新读取并比较文件身份。
  - 任一关键属性变化时更新索引并跳过本次删除。
  - 自动清理实际排序依据与界面文案统一；当前界面写“修改时间”，实现使用“创建时间”。
- 验收标准：
  - 扫描后覆盖同路径文件，再执行删除时必须跳过新文件。
  - 修改文件内容或大小后自动清理不得删除该文件。
  - 审计日志记录“文件已变化，跳过删除”的原因。

### P1-05 文件操作超时后会遗弃卡死线程

- [x] 已修复（2026-08-18）
- 实施结果：删除 `FileOp`/`NetChk` 线程池及“超时后重建线程池”逻辑；本地元数据操作直接执行，UNC/映射盘的存在性、目录创建、递归扫描和磁盘信息查询改由最多 2 个可追踪 PowerShell 子进程执行。
- 超时与停止：子进程到时会被强制结束并回收，停止上传会立即结束所有活动文件操作进程；连续 3 次超时后熔断 30 秒，不再提交新探测。
- 健康状态：新增活动进程数、遗留任务数（固定为 0）、连续超时数和熔断状态；移除所有 `FileOp` 线程，避免解释器退出时等待不可回收线程。
- 验证证据：`pytest -q tests/test_upload_mvc.py tests/test_resume_without_size_limit.py tests/test_lifecycle_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p105`，40 passed；另以 1800 次探测加速模拟 30 分钟连续超时，实际只创建 3 个失败进程后熔断，活动/遗留任务为 0；中文路径扫描与磁盘信息实测通过。
- 位置：`src/workers/upload_worker.py:598-644`
- 问题：`future.cancel()` 无法停止已经运行的文件系统调用；线程池重建后，旧线程仍可能永久阻塞且不再被追踪。长期网络异常可能持续累积线程和内存。
- 修复要求：
  - 不再把“重建线程池”当作终止阻塞调用的手段。
  - UNC/映射盘高风险操作放入可终止子进程，或采用可协作取消的受控实现。
  - 所有后台任务必须可追踪，健康状态应包含遗留任务数。
  - 设置任务并发上限和重复超时熔断策略。
- 验收标准：
  - 连续模拟网络超时 30 分钟，线程数和内存不持续增长。
  - 停止上传后不遗留 `FileOp`/网络探测后台任务。
  - 重连后可恢复上传，不需要重启程序。

### P1-06 退出阶段存在无限等待

- [x] 已修复（2026-08-18）
- 实施结果：Runtime 默认后台执行器改为最多 2 个守护任务，任务卡住不会阻止进程退出；Runtime/Cleanup executor 关闭统一使用 `wait=False, cancel_futures=True`，关闭后禁止新提交。
- 硬超时：UNC/映射盘磁盘空间查询通过独立 PowerShell 进程执行，2 秒超时后返回未知值；不再在线程中直接执行可能永久阻塞的网络盘查询。
- 退出编排：Lifecycle 除 Upload/Cleanup 外，同时检查 FTP 测试和 Runtime 活动任务；总超时后返回明确参与者名称和超时毫秒数，调用 View 恢复界面，不进入最终资源释放。任务自然结束后可再次退出。
- 验证证据：`pytest -q tests/test_runtime_mvc.py tests/test_lifecycle_mvc.py tests/test_cleanup_mvc.py tests/test_cleanup_index.py tests/test_cleanup_policy_and_startup.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p106`，54 passed；覆盖阻塞日志的非阻塞 shutdown、守护线程属性、网络盘硬超时、退出超时恢复及任务自然结束。
- 位置：`src/controllers/runtime_controller.py:80-85`、`src/controllers/lifecycle_controller.py:269-282`
- 问题：最终退出时调用 `executor.shutdown(wait=True)`，没有超时。映射盘磁盘查询或日志操作阻塞时，应用可能永久无法退出。
- 修复要求：
  - 退出前禁止提交新任务并取消未开始任务。
  - 网络磁盘查询必须有可执行的硬超时。
  - 最终资源释放不得进行无界等待。
  - 超时后记录具体参与者和任务，但不得静默卡死。
- 验收标准：
  - 模拟永久阻塞的磁盘查询，退出流程在规定时间内返回并给出错误。
  - 正常退出后无上传、清理、FTP、Runtime 后台线程残留。

## 3. P2：稳定性、内存和功能完整性问题

### P2-01 手工磁盘扫描会一次性创建全部文件控件

- [x] 已修复（2026-08-18）
- 实施结果：文件记录仍为轻量 Model，但 Qt 表格改为每页最多 1000 行；使用 `QTableWidgetItem` 原生勾选状态，不再为每个文件创建独立 `QCheckBox`/容器/Layout。新增上一页、下一页和页码状态。
- 逻辑修复：勾选项直接携带对应 `CleanupFileItem`，搜索、快捷筛选、右键菜单和排序后选择不再依赖“可视行号等于原列表下标”的脆弱假设；全选/取消全选仍作用于全部结果。
- 验证证据：`pytest -q tests/test_view_components.py tests/test_responsive_layout.py tests/test_cleanup_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p201`，26 passed；10 万文件用例只创建 1000 个可视行、100 页、0 个逐文件复选框控件，并验证跨页与全选语义。
- 范围说明：上述 10 万项用例使用轻量记录验证界面规模行为，不等同于真实超大文件端到端上传；真实超大文件测试已接受风险并跳过，不能据此写为通过。
- 位置：`src/services/cleanup_service.py:123-172`、`src/ui/dialogs/disk_cleanup_dialog.py:149-180`
- 要求：使用 `QAbstractTableModel`、分页或有界批次，不得为所有结果创建独立复选框控件；至少验证 10 万文件规模。

### P2-02 上传每轮把全部源文件路径加载到内存

- [x] 已修复（2026-08-18）
- 实施结果：本地目录改为 `os.walk()` 逐文件 `yield`；UNC/映射盘由可终止 PowerShell 扫描进程逐行输出，上传 Worker 边扫描边处理，不再构造全量路径列表。
- 语义保持：扩展名过滤、停止检查、首次发现文件后的延迟、逐文件进度和周期扫描均保留；进度总数随扫描发现动态增长，结束时为本轮实际总数。停止时会结束远程扫描进程。
- 验证证据：`pytest -q tests/test_upload_mvc.py tests/test_resume_without_size_limit.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p202`，34 passed；10 万路径生成器仅消费前 500 个时实际只生成 500 个，证明未预加载余下 99,500 个路径。
- 位置：`src/workers/upload_worker.py:1321-1337`
- 要求：改为流式遍历或固定批次处理；保持取消、过滤、进度和周期扫描语义。

### P2-03 主界面运行日志无限增长

- [x] 已修复（2026-08-18）
- 实施结果：主界面 `QPlainTextEdit` 最多保留 5000 个文本块，磁盘清理对话框最多保留 2000 个；超出后由 Qt 自动丢弃最旧 UI 文本，避免长时间运行持续占用内存。
- 磁盘策略：运行日志继续按日期写入完整 `upload_YYYY-MM-DD.txt`，初始化时清理修改时间超过 30 天的同类日志；单个历史文件删除失败不影响当天日志。
- 验证证据：`pytest -q tests/test_view_components.py tests/test_runtime_mvc.py tests/test_architecture_boundaries.py -p no:cacheprovider --basetemp E:\Python\文件上传\.pytest-tmp-p203`，21 passed；覆盖 UI 上限和 31 天旧日志清理。
- 位置：`src/ui/panels.py:683-686`、`src/ui/main_window.py:3041-3057`
- 要求：通过 `setMaximumBlockCount()` 或环形缓冲限制内存日志；磁盘日志继续完整保留，并增加日志轮转/保留策略说明。

### P2-04 停止上传时归档队列可能未排空

- [x] 已修复（2026-08-18）
- 实施结果：上传成功后先以临时文件 + `fsync` + `os.replace` 原子写入 `data/pending_archives.json`，再把任务放入内存队列；快速停止不再依赖队列排空，未执行任务已持久化。
- 重启语义：启动时恢复未完成归档/删除任务；源扫描会排除已在待归档日志中的文件，避免重复上传。仅当移动或删除真正完成后才删除日志记录；备份盘离线等失败会保留源文件和记录到下次启动。
- 防损坏：待归档日志损坏时采用 fail-closed，不会把损坏日志静默覆盖为空文件；同一源路径在同一 Worker 中只入队一次。
- 验证证据：`pytest -q tests/test_pending_archive.py tests/test_upload_mvc.py --basetemp=.test_runtime\p2_04`，22 passed；覆盖快速停止+重启恢复、扫描排除、归档失败保留、原子替换失败保护旧日志、损坏日志 fail-closed 及关闭备份时持久化删除。
- 位置：`src/workers/upload_worker.py:1207-1242`、`src/workers/upload_worker.py:1610-1612`
- 要求：定义清晰的安全停止语义，排空或持久化待归档任务；不能让已上传源文件在下次启动时被无提示重复上传。

### P2-05 FTPS 服务器证书配置不完整

- [x] 已修复（2026-08-18）
- 配置闭环：`FTPServerSettings`、默认配置、主界面加载/保存/采集和协议启动配置均新增 `cert_file`/`key_file`；界面提供证书与私钥文件选择，仅在 TLS 可用且勾选时可编辑。
- 启动前验证：开启 TLS 时必须同时提供存在的证书和私钥，并通过 `SSLContext.load_cert_chain()` 校验 PEM 内容及公私钥匹配。运行环境缺少 `pyOpenSSL` 时 Service 明确拒绝启动，界面禁用 TLS 开关并显示原因。
- 打包依赖：`requirements.txt` 新增 `pyOpenSSL>=24.0.0`，PyInstaller spec 显式包含 `OpenSSL`/`cryptography` 模块；当前构建环境已安装 pyOpenSSL 26.4.0。
- 验证证据：`pytest -q tests/test_ftp_basic.py tests/test_ftp_mvc.py tests/test_mvc_models.py tests/test_settings_controller.py tests/test_more_menu_permissions.py -p no:cacheprovider --basetemp=.test_runtime\p2_05_full`，38 passed + 5 subtests；包含动态生成自签名证书、真实 FTPS 控制通道与数据通道 TLS 握手及加密上传。
- 位置：`src/services/ftp_service.py:38-58`、`src/ui/main_window.py:1561-1574`
- 要求：增加证书和私钥配置、路径选择及启动前验证；如果暂不支持，隐藏或禁用 TLS 开关并明确提示。

### P2-06 去重算法在大目录下接近 O(源文件数 × 目标文件数)

- [x] 已修复（2026-08-18）
- 实施结果：新增 `DedupIndexRepository`，使用 SQLite `data/dedup_hash_index.db` 持久化目标根目录、路径、算法、大小、修改时间和哈希；扫描使用 generation 标记增量 upsert 并删除已不存在的索引项，不构造全量 `seen` 集合。
- 性能语义：每次 Worker 运行只流式同步一次目标目录元数据；每个源文件先用大小在 SQLite 中查候选，仅对同大小且无有效缓存哈希的文件读取内容。索引跨重启复用，文件大小/修改时间变化时自动失效，新上传文件立即回写索引。
- 网络盘：目标目录同步复用可终止的 PowerShell 流式扫描，不恢复不可终止的 UNC `os.walk` 调用。
- 验证证据：`pytest -q tests/test_dedup_index.py tests/test_upload_mvc.py tests/test_pending_archive.py -p no:cacheprovider --basetemp=.test_runtime\p2_06_final`，27 passed；10 万目标项用例只扫描 1 次，两次查询合计只哈希 1 个同大小候选文件。
- 位置：`src/workers/upload_worker.py:1113-1136`
- 要求：建立持久化或增量哈希索引，先按文件大小筛选，再计算哈希；增加大目录性能测试。

### P2-07 本地账号密码哈希强度不足

- [x] 已修复（2026-08-18）
- 新格式：账号密码改为 `pbkdf2_sha256$iterations$salt$digest`，每次使用 16 字节独立随机 salt、600,000 次 PBKDF2-HMAC-SHA256，并使用常量时间比较；损坏或低迭代格式拒绝登录。
- 兼容迁移：保留旧 64 位 SHA-256 验证路径，旧凭据首次成功登录后立即重算 PBKDF2 并通过配置 Controller 原子写回；写回失败时回滚内存凭据并拒绝登录，不会产生“内存已迁移、磁盘未迁移”的分裂状态。
- 默认口令：默认用户/管理员登录后会先把凭据升级为 PBKDF2，同时锁定上传、配置、FTP 和清理等业务操作，自动打开修改密码对话框；仅保留改密、退出和语言入口。普通用户现可修改自己的密码，不能修改管理员密码。
- 验证证据：`pytest -q tests/test_auth_mvc.py tests/test_more_menu_permissions.py tests/test_view_components.py tests/test_mvc_models.py tests/test_settings_controller.py -p no:cacheprovider --basetemp=.test_runtime\p2_07_final2`，42 passed + 5 subtests；覆盖独立 salt、旧哈希透明迁移/迁移回滚、默认口令强制改密与界面锁定。
- 位置：`src/services/auth_service.py:18-29`
- 要求：使用带独立 salt 的 PBKDF2、scrypt 或 Argon2；保留旧 SHA-256 哈希迁移路径；默认账号首次使用必须明确引导修改。

### P2-08 自动测试依赖宿主磁盘剩余空间

- [x] 已修复（2026-08-18）
- 实施结果：SMB 上传+归档集成用例不再使用会被 Worker 钳制为 5% 的 `disk_threshold_percent=0`；用例使用正常 10% 配置，并显式 mock `UploadWorker._disk_ok()` 返回健康磁盘结果。
- 测试边界：上传用例只验证复制/归档逻辑，低磁盘、未知磁盘及网络盘异常由独立的 Runtime/Cleanup/Upload 用例模拟，测试结果不再受开发机 C/E 盘实际使用率影响。
- 验证证据：`pytest -q tests/test_upload_mvc.py tests/test_runtime_mvc.py tests/test_cleanup_policy_and_startup.py tests/test_cleanup_index.py -p no:cacheprovider --basetemp=.test_runtime\p2_08`，51 passed。
- 位置：`tests/test_upload_mvc.py:450-477`
- 要求：测试中注入磁盘空间提供者或 mock 磁盘检查，不得用 `disk_threshold_percent=0` 假设能关闭实际最低 5% 保护。

## 4. 架构结论：暂不整体切换 MVP

当前项目不是失败的 MVC，也不需要在现场发布前整体重写为 MVP。

已达到的边界：

- Model 不依赖 Qt View。
- Controller 不直接导入具体 UI、Service、Repository、Worker 或 Protocol。
- Service、Repository、Worker 和 Protocol 的依赖方向基本清晰。
- `src/main.py` 是当前唯一生产组合根。

尚未达到“严格 MVC/被动 View”的部分：

- `MainWindow` 仍承担请求组装、配置保存协调、FTP 测试和自动清理调度等职责。
- `DiskCleanupDialog` 直接读取和修改 `parent_window` 内部属性，存在 View 到 View 的强耦合。
- 当前更准确的描述是 **MVC/MVP 混合结构**。

架构执行策略：

- [x] P1/P2 修复期间禁止进行大爆炸式 MVP 重写；本轮未做整体改名或层级重排。
- [x] 保留现有 Model、Service、Repository、Worker 和 Protocol，仅在现有边界中补齐职责。
- [x] 按功能渐进采用 Presenter 职责：现有 `UploadController`、`FTPController`、`CleanupController` 继续作为 View 的语义事件/渲染状态中介，不为名称而新增空转发层。
- [x] View 仅收集输入、发出语义事件并渲染状态；架构边界测试禁止 UI 新增 Service/Repository/Worker/Protocol 导入、文件扫描/删除和直接基础设施 I/O。
- [x] 已移除 `DiskCleanupDialog` 对 `parent_window` 私有属性和私有方法的访问；Qt parent 只保留窗口所有权，设置快照/保存/错误/角色经 `CleanupSettingsGateway` 公开接口传递。
- [x] 保留 Controller 命名；只有未来职责真正变为完全被动 View 编排时再评估 Presenter 改名。

架构验证：`pytest -q tests/test_architecture_boundaries.py tests/test_view_components.py tests/test_responsive_layout.py tests/test_cleanup_mvc.py tests/test_more_menu_permissions.py -p no:cacheprovider --basetemp=.test_runtime\architecture`，37 passed + 5 subtests。

## 5. 正式发布验收门槛

### 自动化与静态检查

- [x] `python -m compileall -q src tests` 通过。
- [x] `pytest -q tests -p no:cacheprovider --basetemp=.test_runtime\full_gate_final` 全部通过：159 passed + 5 subtests，23.26s。
- [x] MVC/架构边界测试全部通过；全量回归包含 9 条架构依赖/线程边界守卫。
- [x] 已安装 pyright 1.1.411；经确认将 Qt 动态 View/测试替身交给架构与运行测试，pyright 覆盖 Model、Controller、Repository、Core、Config、非动态 Service、UploadWorker 和 FTP Protocol；修复 8 个真实 Optional/Protocol/生成器类型问题后结果为 `0 errors, 0 warnings, 0 informations`。因 Windows 拒绝 pyright 创建 `nodejs.exe` 链接，验证使用其已下载的 `node.exe` 直接执行同一 CLI `dist/index.js`。
- [x] 已新增 P1/P2 对应回归，覆盖原子持久化、可取消网络操作、有界退出、10 万项 UI/扫描/去重、归档恢复、真实 FTPS 握手、PBKDF2 迁移、磁盘注入、网络抖动、SQLite 句柄释放和自动清理启用确认。

### 打包验证

- [x] 使用当前本地源码重新执行 PyInstaller 构建（PyInstaller 6.12.0，2026-08-18 16:03:10）。
- [x] 构建产物版本、文件名和界面版本均为 v3.5.1。
- [x] 接受风险并跳过：未在独立无开发环境的干净 Windows 电脑启动；已有隔离目录、清空 `PYTHONHOME/PYTHONPATH`、仅系统 `PATH` 的近似验证不等同于该项验收，用户于 2026-08-18 明确接受剩余风险。
- [x] 冻结包实际创建并写入配置、日志、续传目录、清理索引、去重索引和待归档日志。
- [x] 冻结包探针验证图标、中文/英文资源、PySide6、pyftpdlib、OpenSSL、cryptography、Send2Trash 均可用。
- [x] 冻结包临时生成证书，完成真实 FTPS 控制通道和数据通道 TLS 上传，并释放服务线程。

### 现场模拟

- [x] 接受风险并跳过（真实现场网络）：未在真实现场网络完成 SMB/FTP 正常上传、断网、重连和网络抖动验收；本地上传、UNC 写探针和确定性断开→不稳定→恢复结果不能替代现场网络测试。用户于 2026-08-24 明确接受该风险并跳过，此项不得表述为测试通过。
- [x] 中断上传后续传并仅提交一次、快速停止后的待归档状态重启恢复均有回归测试。
- [x] 目标已有旧文件时模拟替换失败，旧文件、临时文件和续传记录均保留。
- [x] 磁盘不足、配置损坏、配置/日志/审计写入失败路径均有确定性回归。
- [x] 首次启用自动清理必须确认“不受保留天数限制”，默认选择为“否”。
- [x] 自动清理会重新比对文件身份，不删除扫描后被覆盖或更新的文件。
- [x] 10 万项 UI 分页、源文件流式扫描、清理索引批处理和去重候选压力回归通过。
- [x] 接受风险并跳过（真实超大文件）：未使用现场预期最大体量的真实单文件或真实文件集完成端到端上传、续传、归档和资源占用验收。10 万项轻量记录、生成器和普通文件回归不能替代真实超大文件测试；用户于 2026-08-24 明确接受该风险并跳过，此项不得表述为测试通过。
- [x] 接受风险并跳过（24 小时长稳）：未完成至少 24～72 小时墙钟连续运行；最终冻结包仅完成 182.766 秒/181 样本短时基线。真实 24 小时任务曾在约 240 秒主动终止，成品与监控进程均已停止且无残留 PID；用户于 2026-08-24 明确接受剩余时长风险并跳过，中止报告不得作为长稳通过证据。
- [x] 最终冻结包正常退出；残留进程 0、后台 FTPS 线程已释放、标准 FTP 监听端口 0。

## 6. 推荐执行顺序

1. P1-04 清理删除身份校验。
2. P1-03 断点续传原子提交。
3. P1-02 配置原子保存与损坏恢复。
4. P1-01 FTP 异步连接及可取消停止。
5. P1-05 文件操作超时与线程生命周期。
6. P1-06 有界退出。
7. P2 内存、归档、FTPS、去重和认证问题。
8. 完整测试、PyInstaller 构建、干净机验证和现场模拟。

## 7. 完成定义

只有同时满足以下条件，目标才可标记为完成：

- 所有 P1 项已修复并有回归测试。
- P2 项已完成，或逐项记录经用户明确接受的延期理由和现场规避措施。
- 自动化、静态检查、打包验证和现场模拟已通过，或对无法执行的外部验收逐项记录用户明确接受的风险豁免。
- 最终代码审查没有新的 P0/P1 问题。
- 生成最终发布验收报告，记录版本、测试结果、遗留风险和现场部署说明。
