# 图片异步上传工具 v3.5.1 现场发布验收报告

## 结论

**GO（带风险豁免）。本地代码、自动化、静态检查、PyInstaller 构建和冻结包烟雾测试均通过，未发现新的 P0/P1 代码问题。用户于 2026-08-24 对以下三项未执行验收明确接受风险并跳过：**

1. 对最终二进制完成至少 24 小时墙钟连续运行。
2. 使用现场预期最大体量的真实超大文件或真实文件集完成端到端上传、续传、归档和资源占用验收。
3. 在真实现场网络完成 SMB/FTP 正常上传、断网、重连和网络抖动验收。

这三项的状态均为“接受风险并跳过”，不是“测试通过”。现有短时、模拟、回环和轻量压力测试不能替代这三项验收。发布可继续，但现场应保留回退版本并加强首日观察。

另有一项既有风险记录：独立、未安装 Python/开发依赖的干净 Windows 业务冒烟未执行，用户已于 2026-08-18 接受风险并跳过；该项不计入本次三项。

## 交付物

- 程序目录：`dist/ImageUploadTool_v3.5.1`
- 最终压缩包：`dist/ImageUploadTool_v3.5.1-release-20260818-final.zip`
- ZIP 大小：58,779,246 字节
- ZIP SHA-256：`EA58E74440D480BBEAC8ECA6A5F9E131E8ADB29F52A78460BFD2865C2E7C5C15`
- 源文件数：244；解压后总大小：141,221,549 字节
- EXE 构建时间：2026-08-18 16:03:10
- 长稳监控证据：`.test_runtime/packaged_soak_exact_final_20260818_1610.json`
- 冻结包探针证据：`.test_runtime/packaged_soak_exact_final_20260818_1610/release_smoke.json`

## 代码审核与修复结果

- P1-01 至 P1-06 全部完成：GUI 网络阻塞、配置非原子写入、续传提交丢失窗口、清理身份校验、超时任务泄漏和无限退出等待均已修复并有回归。
- P2-01 至 P2-08 全部完成：10 万项 UI/扫描内存、流式源扫描、有界日志、待归档持久化、FTPS 完整配置、SQLite 去重索引、PBKDF2 密码迁移和磁盘状态注入均已落地。
- 发布探针发现并修复了一个额外隐藏问题：去重 SQLite 的事务上下文不会自动关闭连接，Windows 下会锁住数据库并可能长期累积句柄。现已使用 `finally` 关闭，并以数据库移动回归验证句柄已释放。
- 首次启用自动清理新增强制风险确认，明确说明“不受保留天数限制”，默认按钮为“否”。
- 网络抖动新增断开→不稳定→恢复回归，确认只触发一次自动暂停和一次自动恢复。

## 自动化与静态门禁

- `python -m compileall -q src tests tools`：通过。
- `pytest -q tests -p no:cacheprovider --basetemp=.test_runtime\full_gate_final`：159 passed，5 subtests passed，23.26s。
- pyright 1.1.411：0 errors，0 warnings，0 informations。
- 架构守卫持续禁止 UI 直接导入 Service/Repository/Worker/Protocol、直接执行文件扫描/删除或访问清理对话框 parent 私有成员。
- 10 万项测试覆盖 UI 分页、源路径流式消费、清理索引批量写入和去重规模行为。
- 10 万项测试使用轻量记录或生成器验证规模行为，不等同于真实超大文件端到端测试；后者已接受风险并跳过。

## 架构结论

不建议为此次桌面现场发布整体从 MVC 重写为 MVP。项目目前是有明确边界的 MVC/MVP 混合结构：Controller 已承担大部分 Presenter 的界面编排职责，Service/Repository/Worker/Protocol 分层有实际价值。此次采用渐进式策略：

- 保留现有 Controller 命名和公共接口，避免为术语重写稳定业务。
- View 只收集输入、发出语义事件和渲染状态。
- `DiskCleanupDialog` 通过 `CleanupSettingsGateway` 获取/保存设置，不再访问 MainWindow 私有成员。
- 未来只有当 View 进一步完全被动化时，再评估把具体 Controller 改名为 Presenter。

因此，不需要追求“形式上严格 MVC”；更重要的是持续执行当前架构边界测试。

## 冻结包验证

最终构建使用 PyInstaller 6.12.0、Python 3.13.5。成品复制到全新隔离目录后，清空 `PYTHONHOME`、`PYTHONPATH`，并将 `PATH` 缩减为 Windows 系统目录运行。

冻结包 11 项探针全部通过：

- 版本、EXE 文件名和界面标题均为 v3.5.1。
- 图标、中文/英文资源存在。
- PySide6、pyftpdlib、OpenSSL、cryptography、Send2Trash 可导入。
- 配置、日志、`resume_data`、清理索引、去重索引和待归档日志可写。
- 临时生成证书后，真实 FTPS 控制通道和数据通道完成 TLS 上传，内容一致。
- FTPS 服务线程释放；应用协作式退出码为 0；残留进程和标准 FTP 监听均为 0。

该结果是强隔离近似验证，不等同于独立干净 Windows 电脑；对应差异已由用户于 2026-08-18 接受风险并跳过。

## 稳定性与资源结果

最终精确构建连续运行 182.766 秒，每秒采样，共 181 个样本：

- RSS 首段中位数：96,800,768 B
- RSS 尾段中位数：96,714,752 B
- RSS 首尾变化：-86,016 B
- RSS 峰值：96,817,152 B（约 92.33 MiB）
- 线程峰值：8；首尾变化：-6
- 句柄峰值：332；首尾变化：-10
- 日志增长：485 B
- 退出码：0；超时：否；残留 PID：无

短测未显示内存、线程、句柄或日志无界增长，但不等同于 24～72 小时墙钟测试。真实 24 小时任务曾于 2026-08-18 16:10:26 从最终 ZIP 的独立解压副本启动，后在约 240 秒主动终止，监控和成品进程均已停止且无残留 PID。用户于 2026-08-24 对剩余时长风险明确接受风险并跳过；该项未通过 24 小时验收。中止报告 `.test_runtime/wallclock_24h_final_zip_20260818/release-soak-24h.json` 仅用于记录取消原因，不得作为长稳通过证据。可复用命令：

```powershell
python .\tools\release_soak_test.py --exe "<隔离副本>\ImageUploadTool_v3.5.1.exe" --duration-seconds 86400 --sample-interval 30 --output ".\release-soak-24h.json"
```

## 现场场景状态

已通过自动化或冻结包验证：中断续传、原子替换失败保护、配置损坏、低磁盘、日志/审计失败、清理文件身份变化、10 万项轻量记录压力、快速停止后的待归档恢复、FTPS 握手和退出资源释放。上述结果不包含真实超大文件和真实现场网络验收。

真实现场网络测试尚未完成。本机 `\\localhost\E$\Python\文件上传` 和 `\\127.0.0.1\E$\Python\文件上传` 均拒绝访问；申请创建仅用于验收的临时 SMB 共享也被 Windows 拒绝，未创建或修改任何共享。本地上传、回环 FTPS、UNC 写探针和模拟网络抖动均不能替代真实现场网络验收。用户于 2026-08-24 对该项接受风险并跳过，不能表述为测试通过。若现场条件允许，仍建议验证：

1. 目标和备份 UNC 均可写，上传后内容与源文件一致。
2. 上传中断开网络，程序自动暂停且 UI 不冻结。
3. 网络恢复后只恢复一次，不重复上传或重复归档。
4. 抖动期间重试有界，退出后无 PowerShell、Worker、FTP 或 Qt 线程残留。

真实超大文件测试尚未完成。未使用现场预期最大体量的真实单文件或真实文件集验证端到端上传、断点续传、归档耗时、磁盘临时空间和峰值内存。现有普通文件回归、10 万项轻量记录和生成器压力测试不能替代该项。用户于 2026-08-24 对该项接受风险并跳过，不能表述为测试通过。

## 部署说明

1. 校验 ZIP SHA-256 后，将整个目录解压到现场账号可写的本地目录；不要只复制 EXE，也不要直接从 ZIP 内运行。
2. 因配置、日志和数据库默认写在 EXE 同目录，不要部署到普通用户不可写的 `Program Files`。
3. 首次登录后按强制流程修改默认密码，再开放上传、FTP 和清理业务。
4. SMB 使用 UNC 路径，并确认运行账号对目标与备份目录均有创建、替换、重命名和删除权限。
5. 启用 FTPS 时使用匹配的证书/私钥文件，限制私钥 ACL，并在防火墙放行控制端口及实际配置的被动端口范围。
6. 自动清理首次启用时仔细核对目录、格式和删除模式；该策略达到阈值后不受保留天数限制。
7. 保留上一个稳定版本完整目录作为回退，不要让新旧版本同时运行。

## 最终遗留风险与接受记录

- 接受风险并跳过（高）：真实现场网络的 SMB/FTP 权限、断网、重连与抖动行为未执行验收。
- 接受风险并跳过（中）：真实超大文件的端到端上传、续传、归档和资源峰值未执行验收。
- 接受风险并跳过（中）：最终包未执行完整 24 小时墙钟长稳验收。
- 既有接受风险并跳过（中）：尚未在独立干净 Windows 电脑完成启动与业务冒烟。
- 低：PyInstaller 警告主要为 POSIX、macOS 或可选依赖；关键 Windows 依赖已由冻结包实际导入和业务探针验证。

风险接受人：用户。2026-08-24 接受范围为 24 小时长稳、真实超大文件、真实现场网络三项，处理状态均为“接受风险并跳过”，不是“测试通过”。独立干净 Windows 验收继续按 2026-08-18 的既有风险接受记录单独保留。基于上述明确风险接受，本报告发布结论为 GO（带风险豁免）。
