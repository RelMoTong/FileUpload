# MVC 重构后三阶段只读验证计划

## 摘要

本轮只执行测试和输出报告：不修改生产代码、不新增 pytest 文件、不更新版本号。所有 GUI 压力测试在临时复制的运行目录中完成，避免修改当前 `config.json` 或访问其中的真实产线路径。退出策略采用始终等待，不调用 `QThread.terminate()` 或 `Stop-Process`。

已知待验证风险：

- 第二实例当前只唤醒窗口，没有“程序已运行”托盘通知。
- 托盘退出直接调用 `QApplication.quit()`，可能绕过 `LifecycleController`。
- UploadWorker 的主循环、归档线程和网络线程未显式 `join()`。
- Cleanup 删除 Worker 没有取消标志，退出可能长期等待。

## 第一阶段：冷启动、单实例和托盘

- 记录执行前 Git 状态和 Python 进程基线；确认没有正在运行的本程序。
- 创建临时运行副本和临时源/目标/备份目录，生成关闭自动运行、开启通知的测试配置。
- 依次执行：
  - `python -m compileall -q src`
  - 独立导入 `src.models/controllers/services/repositories/ui`
  - `python -X faulthandler -m src.main`，分别捕获 stdout、stderr 和退出码。
- 从第二个终端再次启动相同命令，验证：
  - 第一实例继续运行。
  - 第二实例在限定时间内以退出码 0 结束。
  - 第一实例被恢复并置顶。
  - 系统托盘出现“程序已运行”通知；没有通知则记为失败。
  - 不出现端口占用、共享内存或 `QLocalServer` 崩溃。
- 人工验证托盘图标、右键“显示主窗口”和“退出程序”均可响应，托盘图标生命周期持续到应用退出。

## 第二阶段：线程幽灵压力测试

- 在临时目录中启动应用，使用有效的本地 SMB 测试路径。
- 连续执行 10 轮：开始 → 等待 2 秒 → 暂停 → 恢复 → 停止；每轮等待界面状态回到“已停止”再开始下一轮。
- 每轮记录 PID、线程数、句柄数、状态文本和上传日志行数。以第 1 轮结束后的稳定值为基线；第 2–10 轮不得持续增加线程，句柄不得呈单调增长，停止后不得继续产生上传进度事件。
- 点击右上角 X，验证窗口隐藏、托盘仍存在、进程和上传任务继续运行。
- 启动本机回环 FTP 服务和限速测试文件，在上传进行中从托盘选择退出：
  - X 隐藏阶段 FTP 应继续。
  - 托盘退出后停止接受新任务，等待当前文件/FTP/归档/清理线程自然结束。
  - 进程最终消失，FTP 连接关闭，日志不再增长。
- 观察超过 60 秒仍未退出时，记录为高严重度“退出挂起”；按所选策略不强制杀进程，并停止需要清理该进程的后续 GUI 步骤。

## 第三阶段：现有自动化测试与覆盖缺口审计

- 在新的 pytest 进程中运行 Upload、Cleanup、Lifecycle、架构边界专项测试，再运行完整测试集；使用唯一 `--basetemp` 和 `-p no:cacheprovider`。
- 核对现有测试是否真实覆盖：
  - UploadService 请求验证、状态转换、QThread 生命周期和本地 SMB 上传。
  - UploadWorker FTP/文件失败后的重试次数、退避、耗尽和协议状态保留。
  - CleanupService 磁盘使用率达到阈值时触发、未达到时跳过、磁盘查询异常和清理至目标阈值。
  - LifecycleController 关闭顺序、幂等和参与者异常后的继续释放。
- 预计覆盖缺口会单独报告：目前没有直接测试 UploadWorker 的失败重试，也没有直接测试 `CleanupService.should_trigger()` 的阈值边界；现有 Upload 生命周期测试还会创建 `QCoreApplication`。
- 不新增或修改测试文件，只给出建议测试名称、Mock 边界、输入和断言，供下一轮实现。

## 报告和验收

- 在对话中输出三阶段结果表，包含命令、期望、实际、通过/失败、日志证据和进程指标。
- 缺陷按 Critical/High/Medium/Low 报告，标明文件、函数、影响和建议修复，但不执行修复。
- 测试完成后比较前后 Git 状态，确保仓库文件没有变化；仅清理本轮创建且已经验证路径安全的临时目录和日志。
- 不修改任何公共 API、配置结构、版本号或发布资料。
